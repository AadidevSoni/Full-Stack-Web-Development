// classlist
const h1 = document.querySelector("h1");
console.log(h1.classList);

// add()
h1.classList.add("styles");

// remove()
h1.classList.remove("styles");  

// toggle()
h1.classList.toggle("styles"); //if you have class, it removes it and if you dont have the class, it will add it